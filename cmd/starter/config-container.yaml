logger:
  level: "debug"
  encoding: "console"
  path: "stdout"
  environment: "prod"

public_server:
  enable: true
  endpoint: "localhost"
  port: 8888

application:
  app: "starter"

temporal:
  enable: true
  endpoint: "temporal:7233"
